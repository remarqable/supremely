## What this changes and why

<!-- Plain language. Link the issue it closes, for example "Closes #12". -->

## Checklist

- [ ] `make test` passes.
- [ ] If templates changed, `make css` was run and `app/static/css/app.css` is committed.
- [ ] If the schema changed, a migration is included for the production path.
